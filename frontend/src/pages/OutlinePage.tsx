/** Outline page — scene & plot management for a story (reuses WritePage workspace). */
import WritePage from "./WritePage";

function OutlinePage() {
  return <WritePage />;
}

export default OutlinePage;
