AW_APP_NAME=Auto-Writer
AW_DEBUG=false
AW_DATABASE_URL=sqlite+aiosqlite:///../data/auto_writer.db
AW_CORS_ORIGINS=http://localhost:5173,http://localhost:8080

# Desktop mode (set by scripts/start.* or the release launcher):
# AW_DESKTOP_MODE=1
# AW_STATIC_DIR=/path/to/static
# AW_DATA_DIR=/path/to/user-data
