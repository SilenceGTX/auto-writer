{{ work_info }}

各阶段概述与章节归属：
{% for row in stage_rows %}
- 阶段「{{ row.name }}」（{{ row.numbers_text }}）概述：{{ row.overview }}
{% endfor %}

【当前任务】请只为阶段「{{ target_stage_name }}」生成章节大纲。
本阶段需要生成的章节：{{ chapters_text }}（共 {{ chapter_count }} 章）。
不要输出其他阶段的章节。
必须为上述每一章各返回一条，`chapter_number` 与本阶段章节编号一一对应，JSON 数组长度恰好为 {{ chapter_count }}。
仅返回 JSON 数组，不要包含解释或 Markdown 代码块，格式如下：
[{"chapter_number": 整数, "title": "章节标题", "summary": "本章内容概述"}]
