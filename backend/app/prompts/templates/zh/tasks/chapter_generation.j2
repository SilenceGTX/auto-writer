{{ work_info }}

各阶段概述与章节归属：
{% for row in stage_rows %}
- 阶段「{{ row.name }}」（{{ row.numbers_text }}）概述：{{ row.overview }}
{% endfor %}

【当前任务】请只为阶段「{{ target_stage_name }}」生成章节大纲。
本阶段需要生成的章节：{{ chapters_text }}（共 {{ chapter_count }} 章）。
不要输出其他阶段的章节。

输出要求（必须全部满足）：
1. 仅返回一个完整、可解析的 JSON 对象，不要 Markdown 代码块，不要解释性文字。
2. 根对象格式必须为：{"chapters":[...]}；`chapters` 必须是数组。
3. `chapters` 长度必须恰好为 {{ chapter_count }}；为上述每一章各返回一条，`chapter_number` 与本阶段章节编号一一对应，不得遗漏、不得重复、不得多出。
4. 每个元素格式：{"chapter_number": 整数, "title": "章节标题", "summary": "本章内容概述"}。
5. 必须输出完整闭合的 JSON（所有括号与引号均正确闭合）。若篇幅不够，优先保证结构完整与章节条数齐全，可适当缩短 summary，也绝不可输出半截 JSON。
