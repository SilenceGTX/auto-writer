{{ work_info }}

Stage overviews and chapter assignments:
{% for row in stage_rows %}
- Stage "{{ row.name }}" ({{ row.numbers_text }}) overview: {{ row.overview }}
{% endfor %}

[Current task] Generate chapter outlines only for stage "{{ target_stage_name }}".
Chapters for this stage: {{ chapters_text }} ({{ chapter_count }} chapters total).
Do not output chapters that belong to other stages.

Output requirements (all must be satisfied):
1. Return only one complete, parseable JSON object—no Markdown code fences and no explanatory prose.
2. The root object must be shaped as {"chapters":[...]} where `chapters` is an array.
3. The `chapters` array length must be exactly {{ chapter_count }}; return one entry per chapter listed above, each `chapter_number` matching this stage's chapter numbers with no omissions, duplicates, or extras.
4. Each element must be: {"chapter_number": integer, "title": "chapter title", "summary": "chapter summary"}.
5. The JSON must be fully closed (all brackets and quotes balanced). If space is tight, shorten summaries as needed, but never return truncated JSON and never drop required chapters.
